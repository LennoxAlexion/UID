package DAOs;

import java.util.ArrayList;

import org.json.JSONArray;
import org.json.JSONException;
import org.json.JSONObject;

import android.os.AsyncTask;
import DTOs.Roles;
import Utils.GetJSONUrl;

public class GetRoles  extends AsyncTask<Void, Void, Void>{

	private String url = "http://10.0.2.2/uid-project.org/get_Roles.php";

	/*public void getRoles() {
		try {
			ArrayList<String> temp = new ArrayList<String>();
			JSONArray data = new JSONArray(GetJSONUrl.getJSONUrl((url)));
			for (int i = 0; i < data.length(); i++) {
				JSONObject c = data.getJSONObject(i);
				temp.add(c.getString("Role").toString());
			}
			Roles roles = new Roles();
			roles.setRoles(temp);
		} catch (JSONException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}

	}*/

	@Override
	protected Void doInBackground(Void... params) {
		// TODO Auto-generated method stub
		
		try {
			ArrayList<String> temp = new ArrayList<String>();
			JSONArray data = new JSONArray(GetJSONUrl.getJSONUrl((url)));
			for (int i = 0; i < data.length(); i++) {
				JSONObject c = data.getJSONObject(i);
				temp.add(c.getString("Role").toString());
			}
			Roles roles = new Roles();
			roles.setRoles(temp);
		} catch (JSONException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		
		return null;
	}

}
